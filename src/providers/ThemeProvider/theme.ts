import { createTheme } from '@mui/material'

const theme = createTheme({
  palette: {
    primary: {
      main: '#5754FF',
      light: '#EEEEFF',
    },
    secondary: {
      main: '#46BEA3',
      light: '#EDF9F6',
    },
  },
})

export default theme
